language: emacs

install:
  # Install emacs.
  - "sudo apt-get install emacs23-nox"

# run the tests
script:
  - curl -fsSkL https://raw.github.com/rejeep/carton/master/go | sh
  - bash ~/.carton/bin/carton
  - EMACSLOADPATH=./elpa/test-simple* /bin/sh ./autogen.sh && make check
